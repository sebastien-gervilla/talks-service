export interface Get {
    id: number;
    firstName: string;
    lastName: string;
    biography: string;
}

export interface Create {
    firstName: string;
    lastName: string;
    biography: string;
}